services:
  - type: web
    name: biometric-security-app
    runtime: python
    buildCommand: apt-get update && apt-get install -y cmake && pip install -r requirements.txt
    startCommand: gunicorn app:app
